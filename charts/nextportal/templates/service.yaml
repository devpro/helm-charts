apiVersion: v1
kind: Service
metadata:
  name: {{ include "nextportal.fullname" . }}
  labels:
    {{- include "nextportal.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  selector:
    {{- include "nextportal.selectorLabels" . | nindent 4 }}
  ports:
    - name: http
      port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
