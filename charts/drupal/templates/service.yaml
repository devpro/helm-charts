apiVersion: v1
kind: Service
metadata:
  name: drupal
  labels:
    app: drupal
spec:
  type: ClusterIP
  selector:
    app: drupal
  ports:
    - port: 80
      targetPort: 80
      protocol: TCP
      name: http
